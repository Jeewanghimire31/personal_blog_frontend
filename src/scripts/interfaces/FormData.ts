export interface FormData {
  username?: string;
  email?: string;
  password?: string;
  confirmPassword?: string;
}

export interface BlogForm {
  title?:string,
  description?:string,
  subtitle?:string,
  content?:string,
}